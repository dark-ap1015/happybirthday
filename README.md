## Birthday Cake App

## Setup steps:

#### Cloning:
`git clone https://github.com/dark-ap1015/happybirthday`

#### Install dependencies:
Navigate to project directory or `cd happybirthday` and `npm i` or `npm install`

#### Running the project:
Navigate to project directory or `cd happybirthday` and run `npm start`
